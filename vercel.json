{
  "buildCommand": "prisma generate && next build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["fra1"],
  "env": {
    "NEXTAUTH_URL": "https://${VERCEL_URL}",
    "DATABASE_URL": "${env.POSTGRES_PRISMA_URL}",
    "DATABASE_URL_NON_POOLING": "${env.POSTGRES_URL_NON_POOLING}"
  }
}
