import { Loader2, LucideProps, LogIn, User, Lock, Mail, BookOpen } from "lucide-react";

export const Icons = {
  logo: BookOpen,
  spinner: Loader2,
  user: User,
  lock: Lock,
  mail: Mail,
  login: LogIn,
};
